# ═══════════════════════════════════════════════════════════════════════════════
# K3 Arafah Backend - Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!
# ═══════════════════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────────────────
# 🖥️ SERVER CONFIGURATION
# ───────────────────────────────────────────────────────────────────────────────

# Application environment: development | staging | production
ENV=development

# Port for the HTTP server (default: 8080)
PORT=8080

# ───────────────────────────────────────────────────────────────────────────────
# 🗄️ DATABASE (PostgreSQL) - REQUIRED
# ───────────────────────────────────────────────────────────────────────────────

# Database host (use 'localhost' for local dev, container name for Docker)
DB_HOST=localhost

# Database port (default: 5432)
DB_PORT=5432

# Database username
DB_USER=postgres

# Database password (use a strong password in production!)
DB_PASSWORD=your_database_password

# Database name
DB_NAME=k3arafah_db

# ───────────────────────────────────────────────────────────────────────────────
# 🔐 SECURITY - REQUIRED
# ───────────────────────────────────────────────────────────────────────────────

# JWT secret key for signing tokens (minimum 32 characters!)
# Generate with: openssl rand -hex 32
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_here

# CSRF secret key for token generation (minimum 32 characters!)
# Generate with: openssl rand -hex 32
CSRF_SECRET=your_csrf_secret_key_minimum_32_characters_here

# ───────────────────────────────────────────────────────────────────────────────
# 🔴 REDIS (Caching) - OPTIONAL
# ───────────────────────────────────────────────────────────────────────────────
# If not configured, the application will work without caching (graceful degradation)

# Redis server address (host:port)
REDIS_ADDR=localhost:6379

# Redis password (leave empty if no password)
REDIS_PASSWORD=

# ───────────────────────────────────────────────────────────────────────────────
# ☁️ CLOUDINARY (Media Storage) - REQUIRED for file uploads
# ───────────────────────────────────────────────────────────────────────────────
# Get your credentials from: https://cloudinary.com/console

# Cloudinary URL (format: cloudinary://API_KEY:API_SECRET@CLOUD_NAME)
CLOUDINARY_URL=cloudinary://123456789012345:abcdefghijklmnopqrstuvwx@your_cloud_name

# ───────────────────────────────────────────────────────────────────────────────
# 🌐 CORS CONFIGURATION - REQUIRED
# ───────────────────────────────────────────────────────────────────────────────

# Allowed origin for CORS (your frontend URL)
# Development: http://localhost:3000
# Production: https://yourdomain.com
ALLOWED_ORIGIN=http://localhost:3000

# ═══════════════════════════════════════════════════════════════════════════════
# 📋 QUICK REFERENCE
# ═══════════════════════════════════════════════════════════════════════════════
#
# Development Setup:
#   1. Copy this file: cp .env.example .env
#   2. Update values for your local environment
#   3. Run: go run ./cmd/api -migrate
#   4. Run: go run ./cmd/api
#
# Production Checklist:
#   [ ] ENV=production
#   [ ] Strong JWT_SECRET (32+ chars)
#   [ ] Strong CSRF_SECRET (32+ chars)
#   [ ] Secure DB_PASSWORD
#   [ ] ALLOWED_ORIGIN = your actual domain
#   [ ] Redis configured for caching
#   [ ] HTTPS enabled (handled by reverse proxy)
#
# ═══════════════════════════════════════════════════════════════════════════════
